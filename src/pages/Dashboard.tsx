import DashboardByRole from "@/components/DashboardByRole";

/**
 * Página principal do dashboard
 * Agora usa o componente DashboardByRole que implementa controle de acesso por função
 */
const Dashboard = () => {
  return <DashboardByRole />;
};

export default Dashboard;